{
  "name": "@xreadagent/electron",
  "version": "0.0.5",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "XReadAgent Electron desktop shell",
  "main": "dist/main.js",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "build:check": "tsc -b --noEmit",
    "pack": "node scripts/pack.mjs --dir",
    "pack:mac": "node scripts/pack.mjs --dir --mac",
    "dist": "node scripts/pack.mjs",
    "dist:mac": "node scripts/pack.mjs --mac",
    "pack:python": "node scripts/bundle-python.mjs",
    "generate-icons": "node scripts/generate-icons.mjs",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc -b --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.13.4",
    "electron": "^34.0.0",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
